@model IEnumerable<D2DTechSampleApplication.Models.Technician>

@{
    ViewBag.Title = "Technicians List";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Technicians List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DisplayName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DisplayName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            XXXXXXXX
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TechnicianId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TechnicianId }) |
            @Html.ActionLink("Work Tasks", "Index", "WorkTasksAdmin", new { id=item.TechnicianId }, null)
        </td>
    </tr>
}

</table>
